<UserControl x:Class="SecureChat.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <StackPanel Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="Welcome back" FontSize="24" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Sign in to your account" FontSize="13" Opacity="0.5"
                       HorizontalAlignment="Center" Margin="0,0,0,36"/>

            <TextBlock Text="Username" FontSize="12" Opacity="0.7" Margin="0,0,0,6"/>
            <ui:TextBox x:Name="UsernameBox" PlaceholderText="Enter your username" Margin="0,0,0,16">
                <ui:TextBox.Icon>
                    <ui:SymbolIcon Symbol="Person24"/>
                </ui:TextBox.Icon>
            </ui:TextBox>

            <TextBlock Text="Password" FontSize="12" Opacity="0.7" Margin="0,0,0,6"/>
            <ui:PasswordBox x:Name="PasswordBox" PlaceholderText="Enter your password" Margin="0,0,0,28">
                <ui:PasswordBox.Icon>
                    <ui:SymbolIcon Symbol="Key24"/>
                </ui:PasswordBox.Icon>
            </ui:PasswordBox>

            <ui:Button Content="LOG IN" Appearance="Primary"
                       HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                       Padding="0,10" FontWeight="SemiBold" Margin="0,0,0,12"
                       Click="OnLoginClick"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Don't have an account?" Opacity="0.5" FontSize="12" VerticalAlignment="Center"/>
                <ui:Button Content="Register" Appearance="Transparent" Padding="6,1,6,2" BorderThickness="0" Margin="3,0,0,0" CornerRadius="10"
                           FontSize="12" Click="OnRegisterClick"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
